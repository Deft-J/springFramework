<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.coder.mapper.RecordMapper">
    <!-- 新增记录 -->
    <insert id="saveRecord" parameterType="ChangeRecord">
        insert into record(account, updateTime, category, num)
        values(#{account}, #{updateTime}, #{category}, #{num})
    </insert>
</mapper>